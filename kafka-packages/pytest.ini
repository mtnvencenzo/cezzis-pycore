[pytest]
# Pytest configuration
testpaths = test
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = 
    -v
    --strict-markers
    --tb=short
    --disable-warnings

# Add src directory to Python path
pythonpath = src
